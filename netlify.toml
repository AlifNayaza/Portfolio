[build]
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "netlify/functions"

# CUKUP SATU REDIRECT INI SAJA (Untuk React Router / SPA)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200